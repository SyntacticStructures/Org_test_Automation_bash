pipelines:
  - name: taylorl
    steps:
      - name: taylorl_0001
        type: Bash
        configuration:
          integrations:
            - name: int_email
        execution:
          onExecute:
            - echo hello
            - 2 == 1
          onFailure:
            - send_notification int_email --recipients taylorl@jfrog.com --show-failing-commands true