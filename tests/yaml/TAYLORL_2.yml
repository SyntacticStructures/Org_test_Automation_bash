pipelines:
  - name: TAYLORL_2
    steps:
      - name: TAYLORL_2_1
        type: Bash
        execution:
          onExecute:
            - echo 'step 1'
            - echo "${step_id}"
            - add_run_vairables run_key="${step_id}"
      - name: TAYLORL_2_2
        type: Bash
        configuration:
          inputSteps:
            - name: TAYLORL_2_1
        execution:
          onExecute:
            - echo 'step 2'
            - echo "${step_id}"
      - name: TAYLORL_2_3
        type: Bash
        configuration:
          inputSteps:
            - name: TAYLORL_2_2
        execution:
          onExecute:
            - echo 'step 3'
            - echo "${step_id}"
            - echo "${run_key}"
