template: true
valuesFilePath: ../values/gitRepoValues.yml

resources:
  - name: S_DockerPush_6679_001_GitRepo
    type: GitRepo
    configuration:
      gitProvider: s_gitHub
      path: {{ .Values.GitHub.Org_test_Automation_bash.path }}
      branches:
        include: {{gitBranch}}
  - name: S_DockerPush_6679_001_buildInfo
    type: BuildInfo
    configuration:
      sourceArtifactory: s_artifactory

pipelines:
  - name: S_DockerPush_6679_001
    steps:
      - name: S_DockerPush_6679_001_1
        type: DockerBuild
        configuration:
          affinityGroup: S_DockerPush_6679_001
          # TODO: change to dockerImageName: ${artifactory_registry_url}/test-automation-docker-local/s_dockerpush_5849
          dockerImageName: jpdtaylorl.jfrogdev.org/artifactory/test-automation-docker-local/s_dockerpush_5849
          dockerImageTag: ${run_id}
          dockerFileLocation: "tests/docker"
          inputResources:
            - name: S_DockerPush_6679_001_GitRepo
          integrations:
            - name: s_artifactory
      - name: S_DockerPush_6679_001_2
        type: DockerPush
        configuration:
          autoPublishBuildInfo: true
          forceXrayScan: true
          affinityGroup: S_DockerPush_6679_001
          targetRepository: test-automation-docker-local
          inputSteps:
            - name: S_DockerPush_6679_001_1
          integrations:
            - name: s_artifactory
          outputResources:
            - name: S_DockerPush_6679_001_buildInfo
