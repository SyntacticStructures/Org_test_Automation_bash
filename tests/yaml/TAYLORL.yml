resources:
  - name: mvnRepo
    type: GitRepo
    configuration:
      gitProvider: myBB
      path: maven-hello-world
      branches:
        include: master

pipelines:
  - name: TAYLORL
    steps:
      - name: test_jenkins
        type: MvnBuild
        configuration:
          sourceLocation: "my-app"
          forceXrayScan: "true"
          integrations:
            - name: s_artifactory
          inputResources:
            - name: mvnRepo
