pipelines:
  - name: TAYLORL_000_bash
    steps:
      - name: TAYLORL_000_bash
        type: Bash
        execution:
          onExecute:
            - docker images
            - mkdir emptyDir
            - save_tests emptyDir
